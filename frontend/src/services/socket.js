/**
 * Socket.io client helpers will live here starting in Phase 2 (WebRTC signaling).
 * Phase 1 connects from `RoomPage` directly so lifecycle (join/leave/cleanup) stays explicit.
 */

export { API_URL } from './api'
